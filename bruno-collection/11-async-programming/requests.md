# Requests for module 11-async-programming

- **GET**: /api/data/parallel
  ```bash
  curl -X GET http://localhost:8088/api/data/parallel
  ```

- **GET**: /api/health
  ```bash
  curl -X GET http://localhost:8088/api/health
  ```

- **POST**: /api/tasks/process
  ```bash
  curl -X POST http://localhost:8088/api/tasks/process
  ```

- **POST**: /api/email/send
  ```bash
  curl -X POST http://localhost:8088/api/email/send
  ```

- **POST**: /api/calculate/parallel
  ```bash
  curl -X POST http://localhost:8088/api/calculate/parallel
  ```
