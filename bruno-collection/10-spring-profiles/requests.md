# Requests for module 10-spring-profiles

- **GET**: /api/config
    public Map<String, Object> getConfiguration(
  ```bash
  curl -X GET http://localhost:8088/api/config
    public Map<String, Object> getConfiguration(
  ```

- **GET**: /api/config/profiles
  ```bash
  curl -X GET http://localhost:8088/api/config/profiles
  ```
